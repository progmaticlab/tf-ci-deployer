---
- name: create nexus etc directory
  file:
    path: /opt/nexus
    state: directory

- name: create share directory
  file:
    path: "/home/{{ remote_user }}/zuul"
    state: directory

- name: Ensure docker group is present
  group:
    name: docker
    state: present

- name: Ensure remote user is in docker group
  user:
    name: "{{ remote_user }}"
    state: present
    groups: "docker"

- name: apply nexus config
  template:
    src: nexus.properties.j2
    dest: /opt/nexus/nexus.properties

- name: apply docker-compose.yaml
  template:
    src: docker-compose.yaml.j2
    dest: /opt/nexus/docker-compose.yaml

- name: start nexus service
  docker_service:
    project_src: /opt/nexus

