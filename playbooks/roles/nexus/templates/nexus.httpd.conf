ProxyRequests Off
ProxyPreserveHost On

<VirtualHost *:80>
  ServerName {{ nexus_host }}
  ServerAdmin admin@{{ nexus_host }}

  AllowEncodedSlashes NoDecode

  ProxyPass / http://localhost:8081/ nocanon
  ProxyPassReverse / http://localhost:8081/
  ErrorLog logs/{{ nexus_host }}/error.log
  CustomLog logs/{{ nexus_host }}/access.log common
</VirtualHost>
