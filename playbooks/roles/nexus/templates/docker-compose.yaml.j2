version: '3'

services:
  nexus:
    image: sonatype/nexus3:3.15.2
    ports:
      - "80:8081"
    volumes:
      - nexus-data:/nexus-data
    deploy:
      restart_policy:
        condition: on-failure
        delay: 5s

volumes:
  nexus-data: